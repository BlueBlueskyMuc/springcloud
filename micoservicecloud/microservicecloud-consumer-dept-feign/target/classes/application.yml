server:
  port: 80

# 整合 hystrix 加入内容 begin
feign:
  hystrix:
    enabled: true
# 整合 hystrix 加入内容 end

eureka:
  client:
    register-with-eureka: false # 不向 eureka 注册
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/,http://eureka7002.com:7002/eureka/,http://eureka7003.com:7003/eureka/
